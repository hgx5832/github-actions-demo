name: Simple Commands
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo hello
        run: echo "Hello, World!"

      - name: Show Date
        run: date
      
      - name: List Flies
        run: ls -la